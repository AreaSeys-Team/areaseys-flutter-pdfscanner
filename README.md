# areaseys-flutter-pdfscanner

Take photos to your documents, do magic with OpenCV and save as PDF :)


Flutter plugin:

  (Take a photo or select image from files)
    +
  (cut and interpolate image)
    +
  (apply scanner filters)
    =
  save as PDF.
